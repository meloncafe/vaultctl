[Unit]
Description=Vault Token Auto Renew
# Vault 토큰 자동 갱신
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/bin/vaultctl token auto-renew --quiet

# Environment file (recommended) / 환경변수 파일 사용 (권장)
EnvironmentFile=-/etc/vaultctl/config

# Or specify token directly (not recommended - security risk)
# 또는 직접 토큰 지정 (비권장 - 보안 위험)
# Environment=VAULT_TOKEN=hvs.xxx
# Environment=VAULT_ADDR=https://vault.example.com
